# Resolve the installed NVM npx once during reviewed setup; do not use a shell.
OOS_NPX=/home/aitrading/.nvm/versions/node/REPLACE_WITH_INSTALLED_VERSION/bin/npx
PATH=/home/aitrading/.nvm/versions/node/REPLACE_WITH_INSTALLED_VERSION/bin:/usr/local/bin:/usr/bin:/bin
